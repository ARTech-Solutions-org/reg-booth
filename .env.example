# Server Port
PORT=8080

# Security
SESSION_SECRET=event-checkin-secure-random-secret-key-2026
ORGANIZER_PASSWORD=welcome123

# Database (Optional - defaults to local SQLite persistent file in ./data/event.db)
# DATABASE_URL=postgres://user:password@localhost:5432/event_db
